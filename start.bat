@echo off
REM This batch file runs the Python script search_algorithms.py

REM Check if Python is installed by checking the version
python --version

REM Run the Python script
python search_algorithms.py

REM Pause the script to keep the window open after execution
pause
